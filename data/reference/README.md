# Reference data

This directory will contain the verified AESR reference geography, attributes and field dictionary.

## QA note

The currently downloaded web-service layer contains 60 source polygon records but 59 unique AESR codes because AESR 15.3 occurs in two source parts while AESR 20.2 is absent from that source extract. The final national analytical reference should not be described as a verified 60-identity spatial layer until AESR 20.2 is resolved from an authoritative source.
